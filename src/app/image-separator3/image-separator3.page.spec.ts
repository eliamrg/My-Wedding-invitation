import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ImageSeparator3Page } from './image-separator3.page';

describe('ImageSeparator3Page', () => {
  let component: ImageSeparator3Page;
  let fixture: ComponentFixture<ImageSeparator3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ImageSeparator3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
