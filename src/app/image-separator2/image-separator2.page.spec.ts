import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ImageSeparator2Page } from './image-separator2.page';

describe('ImageSeparator2Page', () => {
  let component: ImageSeparator2Page;
  let fixture: ComponentFixture<ImageSeparator2Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ImageSeparator2Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
